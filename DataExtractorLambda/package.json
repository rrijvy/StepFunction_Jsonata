{
  "name": "data-extractor-lambda",
  "version": "1.0.0",
  "description": "Data extraction lambda with JSONata query processing support",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.145"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3"
  }
}
